pipeline{
    agent any

    stages{
        stage("execute"){
            steps{
                sh 'chmod 777 file1.sh'
                sh 'chmod 777 file2.sh'
            }
        }
        stage("build"){
            steps{
                sh './file1.sh'
                sh './file2.sh'
            }
        }
    }
}
